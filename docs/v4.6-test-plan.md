# v4.6 P0 — Test Plan Artifact

> Generated by `/autoplan v4.6` Phase 3 Section 3 / 2026-04-29 / Branch: main
> Companion: [v4.6-autoplan-review.md](v4.6-autoplan-review.md)

## サマリ

| 指標 | 値 |
|---|---|
| 現状 | 293 unit / 7 E2E (16 test files) |
| 追加目標 | **55 unit + 3 E2E** （= 53 当初目標 + 2件 二重計上回帰） |
| 完了時 | 348 unit / 10 E2E |

| モジュール | ファイル | Unit 数 | 優先度 |
|---|---|---:|---|
| T-2: 5/19年ルール（重複期間計算） | `src/lib/tax/overlap.test.ts` (新規) | 30 | P0 |
| C-3: SWR (委譲ファサード) | `src/lib/swr/engine.test.ts` (新規) | 10 | P0 |
| F-2/T-10: 退職翌年住民税 + **二重計上回帰** | `src/lib/simulation/engine.test.ts` (追記) | 5 + 2 = 7 | P0 |
| T-7: ふるさと納税 | `src/lib/tax/furusato.test.ts` (新規) | 8 | P0 |
| **小計 unit** | | **55** | |
| E2E | `e2e/app.spec.ts` (追記) | 3 | P0 |

---

## 1. T-2: 5/19年ルール (`src/lib/tax/overlap.test.ts`)

### A) 境界テスト (13件)

| # | gap | primary y / secondary y | rule | 期待 |
|---|---|---|---|---|
| 1 | 0 (同年) | 30/20 | 5 | overlap=20, secondary effective=0, **80万下限保証** |
| 2 | 4 | 30/20 | 5 | overlap=16, secondary effective=4 |
| 3 | 5 | 30/20 | 5 | overlap=0, フル控除 |
| 4 | 6 | 30/20 | 5 | overlap=0 |
| 5 | 18 | 30/20 | 19 | overlap=2, secondary effective=18 |
| 6 | 19 | 30/20 | 19 | overlap=0, フル控除 |
| 7 | 0 | 0/20 | 5 | overlap=0 (min=0), 両方80万下限 |
| 8 | 5 | 30/0 | 5 | secondary 80万下限 |
| 9 | 0 | 0/0 | 5 | 両方0 → 80万下限のみ |
| 10 | 1 | 20/20 | 5 | overlap=19, secondary effective=1 |
| 11 | 5 | 1/1 | 5 | フル控除 → 控除=80万 |
| 12 | 5 | 30/20 | 5 | direction=iDeCo→退職金 で正しい rule適用 |
| 13 | 19 | 30/20 | 19 | direction=退職金→iDeCo で正しい rule適用 |

### B) End-to-end 税額テスト (4件)

| # | シナリオ | 期待 |
|---|---|---|
| 14 | 退職金1500万+iDeCo800万, 勤続30y/iDeCo20y, gap=0 | 重複控除 → 税>0 |
| 15 | 同, gap=5 | フル控除 → 税大幅減 |
| 16 | 同, gap=19 (退職金先) | フル控除 |
| 17 | iDeCo小額(80万以下) | tax=0 (gap無関係) |

### C) 勤続年数控除切替 (3件)

| # | 内容 |
|---|---|
| 18 | yearsOfService=20 → 40万/年 |
| 19 | yearsOfService=21 → 70万/年に切替 |
| 20 | yearsOfService=1 → 80万下限保証 |

### D) 受給年齢制約 (3件)

| # | 内容 |
|---|---|
| 21 | receiveAge=60 → 受理 |
| 22 | receiveAge=75 → 受理 |
| 23 | receiveAge=59 OR 76 → エラー |

### E) ruleYears 切替 (2件)

| # | 内容 |
|---|---|
| 24 | ruleYears=5 と 19 で同 gap の結果が異なること |
| 25 | tax-config-2026 の `idecoLumpSumOverlapRule` から正しく取得 |

### F) 典型ユーザーシナリオ (3件)

| # | 内容 |
|---|---|
| 26 | 会社員35年 + iDeCo25年 |
| 27 | 公務員30年 + iDeCo20年 |
| 28 | 自営業0年 + iDeCo30年 (退職金なし → idecoTiming無効) |

### G) 処方箋 idecoTiming 軸の独立性回帰 (2件)

| # | 内容 |
|---|---|
| 29 | idecoConfig 未指定 → 既存4軸結果が **完全一致** (seed固定) |
| 30 | idecoTiming 軸 enabled で 4軸結果が変化 (二層探索の交互作用) |

---

## 2. C-3: SWR (`src/lib/swr/engine.test.ts`)

### A) 境界 (4件)

| # | シナリオ | 期待 |
|---|---|---|
| 1 | 資産1億, 期間30年, target=0.90 | rate ≈ 3-5% |
| 2 | 資産0 | maxAnnualExpense = 0 |
| 3 | target=1.0 | rate < Bengen 4% |
| 4 | binary search 収束 | \|successRate - 0.90\| ≤ tolerance |

### B) 委譲一致 (3件)

| # | 内容 |
|---|---|
| 5 | calcSWR の結果と prescription expense 軸の最適値が ±10万円以内で一致 |
| 6 | 同一 seed で再現性 |
| 7 | 委譲先（prescription helpers）が export されていること |

### C) Property (3件)

| # | 内容 |
|---|---|
| 8 | 資産↑ → maxAnnualExpense↑ (単調) |
| 9 | target↑ → maxAnnualExpense↓ (単調) |
| 10 | vsBengen4Pct の符号が日本税制で負（4%より低い） |

---

## 3. F-2/T-10: 退職翌年住民税 (`src/lib/simulation/engine.test.ts` 追記)

### A) 基本動作 (3件)

| # | シナリオ | 期待 |
|---|---|---|
| 1 | salary=600万, retirementAge=50, age=51 | residentTaxLag ≈ `calcAnnualTax(600万,50).residentTax` |
| 2 | salary=0 (無収入退職) | residentTaxLag = 0 |
| 3 | age ≠ retirementAge+1 | 追加なし |

### B) **🔴 CRITICAL 二重計上回帰** (2件 — 新規追加)

| # | シナリオ | 期待 |
|---|---|---|
| 4 | retirementAge=50 のシミュレーション全体で、`taxBd.residentTax` 全年合計が「**在職全年の住民税1年分ずつのみ計上**」と一致（二重計上なし） |
| 5 | 退職前年 (`age=49`) の `residentTax` が 当年に1回 **AND** 退職翌年 (`age=51`) のラグに1回 → **どちらか1回のみ**（重複ゼロ）であることを確認 |

### C) 既存シード固定回帰 (2件)

| # | 内容 |
|---|---|
| 6 | 既存テスト `runSimulation seed=42` の中央値・成功率が変化しないこと |
| 7 | 配偶者ありケース `spouseResidentTax` との混同なし |

---

## 4. T-7: ふるさと納税 (`src/lib/tax/furusato.test.ts`)

### A) 境界 (5件)

| # | taxableIncome | marginalRate | 期待 |
|---|---|---|---|
| 1 | 0 | 0.05 | 2,000 (自己負担) |
| 2 | 1,950,000 | 0.05 | 式通り |
| 3 | 3,300,000 → 0.20 境界 | 0.20 | ブラケット境界 |
| 4 | 6,950,000 → 0.23/0.33 境界 | 0.33 | ブラケット境界 |
| 5 | 40,000,000+ | 0.45 | 高額上限 |

### B) Property (2件)

| # | 内容 |
|---|---|
| 6 | taxableIncome↑ → limit↑ (単調) |
| 7 | 自己負担2000円下限 |

### C) `calcAnnualTax` marginalRate 露出 (1件)

| # | 内容 |
|---|---|
| 8 | `calcAnnualTax` 戻り値に `marginalIncomeTaxRate` が含まれ、`calcFurusatoLimit` が再計算なしで利用可能 |

---

## 5. E2E (`e2e/app.spec.ts` 追記、3件)

| # | フロー | 検証 |
|---|---|---|
| E1 | iDeCo 受給年齢を変更 → 処方箋に「iDeCo一時金を○歳に遅らせると税△万円減」カード出現 | カード DOM 存在 + 数値>0 |
| E2 | QuickStart 入力 → 結果画面 → 年次表で `retirementAge+1` 行に住民税ラグが表示 | 行存在 + 数値>0 |
| E3 | QuickStart → 結果画面 → 「安全な年間支出（SWR）」が**専用タブ/カード**に表示 (User Gate B 採用時) | 数値>0 |

---

## カバレッジギャップリスク

| 未テスト領域 | 理由 | リスク |
|---|---|---|
| iDeCo 併用 (lumpSum + pension) 按分 | v4.6 では lumpSum のみ実装、mixed は P1 | 中 |
| ふるさと納税 × 住宅ローン控除 相互作用 | 住宅ローン控除は P2 (v4.8) | 低 |
| SWR × 配偶者合算 | 配偶者モデル既存だが結合未検証 | 中 |
| 住民税の均等割・調整控除精密計算 | 簡易式で ±数千円誤差許容 | 低 |
| FormState v5→v6 migration（**Critical Bug #2 関連**） | 別途 4件のmigrationテスト必須（本テストプラン外） | **高** |

## ミューテーションテスト ホットスポット

| 関数 | 変異 | 殺傷テスト |
|---|---|---|
| `calcEffectiveYearsForLumpSum` | `>=` → `>` (off-by-one) | #3 (gap=5) と #6 (gap=19) |
| `calcEffectiveYearsForLumpSum` | `Math.min - gap` → `+ gap` | #1 vs #2 差分 |
| `calcFurusatoLimit` | `+ 2000` → `- 2000` | #1 (income=0 → 2000) |
| `calcFurusatoLimit` | `1.021` 係数除去 | #2-#5 ブラケット境界 |
| `calcSWR` 収束 | `<=` → `<` | #4 |
| 住民税ラグ条件 | `age === retirementAge+1` → `age === retirementAge` | #1 vs #3 排他検証 |
| 二重計上 | retirementAge 年の `residentTax` 計上を残す | **回帰 #4 (二重計上)** で殺傷 |

---

## 追加: FormState migration 専用テスト (Critical Bug #2)

レビューで検出された CRITICAL #2（v5→v6 drop型migration）の対応テスト 4件を別途追加。本テストプランの 55+3 とは独立だが、v4.6.0 の最初のコミット内で同梱必要。

| # | ファイル | 内容 |
|---|---|---|
| M-1 | `src/lib/form/storage.test.ts` | v5 stored data → loadFormFromStorage で v6 に migrate されること |
| M-2 | 同 | migrate 後 idecoConfig が default `undefined`、retirementBonus.receiveAge が undefined |
| M-3 | `src/lib/form/url-share.test.ts` | v5 URL → importFormFromJSON で v6 化 |
| M-4 | 同 | v6 URL を v5 で開いた場合のフェイルセーフ（unknown field strip） |

→ **総追加テスト: 55 unit (P0) + 4 unit (Critical) + 3 E2E = 62件追加** で v4.6 完了時 355 unit / 10 E2E。
