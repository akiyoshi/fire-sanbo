# FIRE参謀 — デザインドキュメント

> **バージョン**: v4.6.2
> **更新日**: 2026-04-29
> **ライブ**: https://akiyoshi.github.io/fire-sanbo/

ナビゲーション: [README](README.md) · **DESIGN** · [ARCHITECTURE](ARCHITECTURE.md) · [TODOS](TODOS.md) · [Archive](docs/archive/)

このドキュメントはプロダクトの **ビジョン・設計判断・原則** を記述します（Diataxis: Explanation）。

- 現在どう動いているか → [ARCHITECTURE.md](ARCHITECTURE.md)
- 今後の改善計画 → [TODOS.md](TODOS.md)
- 過去の意思決定の経緯 → [docs/archive/](docs/archive/)

---

## ビジョン

**日本の税制・社会保障を正確に組み込んだ、個人向けライフタイム資産シミュレーター。**

「成功確率 67%」で終わらない — **支出削減・退職延期・収入増加・アロケーション最適化の 4 軸で「90% にするには？」を逆算する処方箋エンジン**を中核に据える。

年金・退職金・ライフイベント・世帯収支・ポートフォリオ最適化を統合した「人生の資産設計エンジン」。

---

## 想定ユーザー

- 30〜50 代の個人投資家（FIRE / セミリタイア計画中）
- 配偶者あり / なし両対応
- 「漠然と不安」よりも「90% にする具体策」が欲しい層
- ブラウザだけで完結したい（インストール・ログイン不要）

---

## プロダクト原則

### 1. 数字の根拠を隠さない

成功率を一つの数字で押し付けない。p5 / p25 / p50 / p75 / p95 を見せ、「最悪ケースで何が起きるか（暴落型 / 資金不足型 / ライフイベント集中型 / 長寿リスク型）」を分類して提示する。計算根拠書（15 セクション・出典リンク付き）で全税計算を開示する。

### 2. 行動可能な答えを返す

「あなたは失敗します」で終わらない。**処方箋エンジン** が 4 軸で目標成功率（デフォルト 90%）に届く調整値を提案する。難易度タグ（やさしい / ふつう / むずかしい）を付け、最高インパクトのカードを視覚的に強調。アロケーション処方箋はワンクリック適用可能。

### 3. サーバーレス・プライバシーファースト

全データはブラウザ内（localStorage + URL フラグメント）に閉じる。サーバーに送信しない。共有は DeflateRaw + Base64url 圧縮で URL に格納。ログイン・トラッキング・分析は導入しない。

### 4. 後方互換を維持する

新フィールドはすべてオプショナル、デフォルト値で旧バージョンの動作を保つ。FormState はスキーマバージョニング（v2 → v3 → v4 → v5）でマイグレーション。共有 URL は古いスキーマも復元できる。

### 5. 安定版を採用し、最前線を追わない

ツールチェーン（typescript, eslint, vite）はパッチのみ自動更新。canary / rc は禁止。本プロジェクトが使っていない新機能のためにメジャーバンプの破壊的変更を受け入れない。詳細は [ARCHITECTURE.md §15](ARCHITECTURE.md#15-依存関係ポリシー)。

### 6. テスト駆動の計算エンジン

`src/lib/` は React 非依存の純 TypeScript。税エンジン・シミュレーション・最適化はそれぞれ独立してテスト可能（v4.6.2 時点で 332 ユニットテスト + 6 E2E）。UI を経由せずロジック単体で検証できる。

---

## 主要設計判断と理由

### 判断: モンテカルロ採用（決定論的計算ではない）

**理由**: リターンは log-normal、相関は共分散行列で表現するのが妥当。決定論的「期待値だけ」では p5 / p95 が出せず「最悪ケースで何が起きるか」を見せられない。1,000 試行で十分な収束 + Worker でオフスレッド実行。

### 判断: 取り崩し最適化は全順列評価

**理由**: 5 口座（NISA / 特定 / iDeCo / 金 / cash）、cash 末尾固定で残り 4! = 24 通りのみ。ヒューリスティックではなく決定論的中央値リターンで全評価できる規模。<50ms 同期計算で UI ブロックなし。iDeCo 60 歳未満除外は確定拠出年金法の制約。

### 判断: ポートフォリオ最適化はディリクレサンプリング

**理由**: 解析的な平均分散最適化は短期売り（負のウェイト）を生むが、個人投資家には不適切。0–1 の重み制約下で効率的フロンティアを近似するにはディリクレ分布によるモンテカルロ（10,000 サンプル）が単純で頑健。

### 判断: 含み益は CostBasis クラスで動的追跡

**理由**: 静的 `gainRatio` 初期値固定では、長期保有でリターンが累積しても含み益率が変わらないため税負担が過小評価される。CostBasis を年次更新することで「積立は含み益 0%」「リターンで含み益率上昇」「按分取り崩し」が自然に表現できる（v4.5.1）。

### 判断: 結果画面は 1 カラム + 1 軍 / 2 軍

**理由**: 2 カラムレイアウトはモバイルで破綻し、ユーザーが「次に何を見るべきか」が不明瞭になる。1 軍（成功率・最優先アクション・チャート・What-if・最悪ケース）を常時表示、2 軍（処方箋・取り崩し・アロケーション・税内訳）は `<details>` で折りたたみ。情報の優先順位を視覚階層に対応させる。

### 判断: 共有 URL はサーバーレス（フラグメント圧縮）

**理由**: ログイン不要 + プライバシー + インフラ不要を全て満たすには URL に全データを格納するしかない。pako の DeflateRaw + Base64url で典型的な FormState は 500–1500 文字に収まる。decompression bomb 対策として圧縮 50KB / 展開 500KB 上限。

### 判断: テンプレートセレクターは初期 5 種で固定

**理由**: 「無限のテンプレート」より「典型 5 ケース（転職 / 住宅購入 / 教育費 / 早期退職 / 年金繰下げ）」を網羅する方がオンボーディング負荷が低い。delta マージ方式により、既存 FormState を保ちつつ関連フィールドのみ上書きできる。

### 判断: 税エンジンは年度切替を抽象化（v3.0 で基盤実装）

**理由**: 税制は毎年微調整される。エンジン本体を年度依存にせず、`getTaxConfig(year)` で JSON を切り替える設計にすれば、新年度対応は JSON 追加のみで済む。`SimulationInput` に `fiscalYear` を持たせ、年度跨ぎは「開始年度で全期間固定」とする（将来の税制変更を予測しない、免責条項に明記）。

### 判断: ESLint flat config + ESLint 9 系で固定

**理由**: ESLint 10 は TS パーサーと react-hooks プラグインの安定版が未追従。`defineConfig()` + `globalIgnores()` への書き換えコストに見合うメリットがない。`~9.39.4` で固定、Dependabot のメジャーバンプは ignore（[ARCHITECTURE.md §15](ARCHITECTURE.md#15-依存関係ポリシー)）。

### 判断: CHANGELOG ファイルは履歴アーカイブのみ、能動的に更新しない

**理由**: ユーザー判断で「git log で十分」。`CHANGELOG.md` は v4.5.7 時点の履歴を残しているが、`/ship` フローで生成しない。バージョン履歴は git タグ + コミットメッセージで参照する。

---

## スコープ境界

### スコープ内

- 日本居住者の所得税・住民税・社保料・退職所得控除・特定口座譲渡益税・金現物譲渡税
- NISA 年間 360 万 / 生涯 1,800 万枠管理
- 厚生年金・国民年金・繰上げ / 繰下げ
- 配偶者あり世帯（年齢差・退職時期差）— シミュレーションエンジン・ FormState スキーマは対応済み。**ウィザード UI は未配線**（現状は JSON インポート / 共有 URL 経由でのみ設定可能 — [TODOS.md](TODOS.md) 参照）
- ライフイベント（一時支出）
- 効率的フロンティア + 目標アロケーション + リバランス
- 個人ユーザー向けブラウザ単体ツール

### スコープ外（恒久）

[TODOS.md の凍結セクション](TODOS.md#-凍結恒久-no-go) を参照:

- PWA 化 / Electron 化
- リブランド・名前変更
- BtoB 展開・法人向け
- ログイン・サーバーサイド機能
- 海外居住者 / 海外税制
- リアルタイム市場データ統合（手入力前提）

---

## 設計のメタ原則

### 過剰機能を避ける

機能要望は「必要性が明確になるまで凍結」が基本姿勢。v4.3 のメンテナンスモード宣言（v4.5.7 で解除）の経緯はこの姿勢を反映している。新機能は [TODOS.md](TODOS.md) のバックログで優先度を付け、P1 以下は「明確なユーザー価値」が説明できるまで着手しない。

### ドキュメントの役割分離

| ファイル | 役割 |
|---------|------|
| `README.md` | 入り口。機能サマリ + セットアップ |
| `DESIGN.md`（このファイル） | ビジョン・原則・設計判断 |
| `ARCHITECTURE.md` | 現在の実装（コード地図） |
| `TODOS.md` | 未着手の改善案（優先度付き） |
| `CHANGELOG.md` | 履歴アーカイブ（能動更新なし） |
| `AGENTS.md` / `CLAUDE.md` | エージェント向けの作業ルール |
| `docs/archive/` | 過去の大規模設計判断の経緯（履歴） |

「実装済みの機能リスト」は ARCHITECTURE.md に統合済み。DESIGN.md には機能の **羅列** ではなく **採用理由** だけを残す。
